Basic Coding Language:
- Used for calculations of numbers
- Maybe create a html calculator app that uses this script
- High-performance for memory usage
- Compiled, makes it a mess but quick

_int32 and int64,
same for floats
lrgint for large integers for arbitrary precision

+,-,*,/,%
^,&,!
sin, cos, tan, arcsin, etc.
for, while
if, else, elif
++

int32 y: 6;
func func_name(int: par) >> int {
	exp par * 6 (exp:=export)
}

- Need something simpler than print:
show int > y

for (int32 i: 0; i < 10; i: i + 1) {
    show(int32) i;
}

int32 i: 0;
while (i < 10) {
    show(int32) i;
    i: i + 1;
}